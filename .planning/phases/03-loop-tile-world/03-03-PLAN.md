---
phase: 03-loop-tile-world
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/scenes/GameScene.ts
  - src/scenes/PlanningOverlay.ts
  - src/scenes/ShopScene.ts
  - src/scenes/RestSiteScene.ts
  - src/scenes/EventScene.ts
  - src/scenes/TreasureScene.ts
  - src/scenes/BossExitScene.ts
  - src/ui/LoopHUD.ts
  - src/ui/TileVisual.ts
  - src/ui/LoopCelebration.ts
  - src/main.ts
autonomous: false
requirements:
  - LOOP-01
  - LOOP-02
  - LOOP-03
  - LOOP-04
  - LOOP-05
  - LOOP-06
  - LOOP-07
  - LOOP-08
  - TILE-01
  - TILE-02
  - TILE-03
  - TILE-04
  - TILE-05
  - SPEC-01
  - SPEC-02
  - SPEC-03
  - SPEC-04
  - SPEC-05

must_haves:
  truths:
    - "Hero visibly autoscrolls through tiles at ground level with camera follow"
    - "HUD shows loop counter, tile points, gold, meta-loot, HP bar, and difficulty multiplier"
    - "Loop completion shows celebration text and tile points earned"
    - "Planning overlay displays miniature loop layout and tile inventory for tile placement"
    - "Adjacency synergy indicators appear on tiles with active synergies in planning view"
    - "Shop overlay provides buy cards, remove cards, reorder deck, buy relics, sell tiles"
    - "Rest overlay shows 3 choices (rest/train/meditate) with immediate result feedback"
    - "Event overlay shows narrative text and 2-3 choices with outcome"
    - "Treasure overlay shows loot items with Take All button"
    - "Boss exit overlay shows two-panel choice: Exit Run (green) vs Continue (red)"
    - "All overlay scenes pause GameScene and resume on close"
  artifacts:
    - path: "src/scenes/GameScene.ts"
      provides: "Thin Phaser scene wrapping LoopRunner, renders tiles, hero, handles autoscroll"
      contains: "LoopRunner"
    - path: "src/scenes/PlanningOverlay.ts"
      provides: "Planning phase UI with miniature loop grid and tile inventory panel"
      contains: "PlanningOverlay"
    - path: "src/scenes/ShopScene.ts"
      provides: "Shop overlay with deck management, relic purchasing, tile selling"
      contains: "ShopSystem"
    - path: "src/scenes/RestSiteScene.ts"
      provides: "Rest site overlay with 3-choice cards"
      contains: "applyRestChoice"
    - path: "src/scenes/EventScene.ts"
      provides: "Event overlay with narrative text and choices"
      contains: "resolveEventChoice"
    - path: "src/scenes/TreasureScene.ts"
      provides: "Treasure overlay with loot display and Take All"
      contains: "openTreasure"
    - path: "src/scenes/BossExitScene.ts"
      provides: "Boss exit choice overlay with two-panel layout"
      contains: "getBossExitChoiceData"
    - path: "src/ui/LoopHUD.ts"
      provides: "HUD component showing loop/gold/tilePoints/metaLoot/HP/difficulty"
      contains: "LoopHUD"
    - path: "src/ui/TileVisual.ts"
      provides: "Reusable tile rendering component (80x80 rect with color/icon/synergy indicators)"
      contains: "TileVisual"
  key_links:
    - from: "src/scenes/GameScene.ts"
      to: "src/systems/LoopRunner.ts"
      via: "GameScene.update() calls loopRunner.tick(delta)"
      pattern: "loopRunner\\.tick"
    - from: "src/scenes/GameScene.ts"
      to: "EventBus"
      via: "Listens for tile-entered, loop-completed, combat-start, open-scene events"
      pattern: "eventBus\\.on"
    - from: "src/scenes/PlanningOverlay.ts"
      to: "src/systems/LoopRunner.ts"
      via: "Calls loopRunner.placeTile() and loopRunner.confirmPlanning()"
      pattern: "loopRunner\\.placeTile"
    - from: "src/scenes/ShopScene.ts"
      to: "src/systems/ShopSystem.ts"
      via: "Calls ShopSystem.buyCard(), removeCard(), sellTile(), etc."
      pattern: "ShopSystem\\."
    - from: "src/scenes/BossExitScene.ts"
      to: "src/systems/LoopRunner.ts"
      via: "Calls loopRunner.onBossChoice('exit'|'continue')"
      pattern: "loopRunner\\.onBossChoice"
---

<objective>
Build all Phaser scenes (GameScene, PlanningOverlay, 5 special tile overlays, BossExitScene) plus UI components (LoopHUD, TileVisual, LoopCelebration). Wire LoopRunner and all special tile systems to their respective scene wrappers. Register all scenes in main.ts.

Purpose: This plan is the visual layer that brings the pure TS systems (Plans 01+02) to life. Every scene is a thin wrapper that reads/writes RunState and delegates logic to the pure systems. The player can now see the loop, place tiles, and interact with all special tiles.

Output: 7 scene files, 3 UI components, updated main.ts. Visual checkpoint confirms the full loop gameplay works.
</objective>

<execution_context>
@C:/Users/nicolas.pereira/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nicolas.pereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-loop-tile-world/03-CONTEXT.md
@.planning/phases/03-loop-tile-world/03-RESEARCH.md
@.planning/phases/03-loop-tile-world/03-UI-SPEC.md
@.planning/phases/03-loop-tile-world/03-01-SUMMARY.md
@.planning/phases/03-loop-tile-world/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From src/systems/LoopRunner.ts:
```typescript
type LoopState = 'idle' | 'traversing' | 'tile-interaction' | 'planning' | 'boss-choice' | 'run-ended';
export class LoopRunner {
  constructor(emit: (event: string, data: any) => void);
  getState(): LoopState;
  startRun(runState): void;
  tick(delta: number): void;
  resumeTraversal(): void;
  confirmPlanning(): void;
  onBossDefeated(): void;
  onBossChoice(choice: 'exit' | 'continue'): void;
  placeTile(slotIndex: number, tileKey: string): boolean;
  getActiveBuffs(): SynergyBuff[];
}
```

From src/systems/TileRegistry.ts:
```typescript
export function getTileConfig(key: string): TileConfig;
export function getAllPlaceableTiles(): TileConfig[];
export function createBasicLoop(length: number): TileSlot[];
export interface TileConfig { type: TileSlotType; terrain?: TerrainType; name: string; color: number; canPlaceManually: boolean; tilePointCost: number; icon: string; }
export interface TileSlot { type: TileSlotType; terrain?: TerrainType; defeatedThisLoop: boolean; }
```

From src/systems/SynergyResolver.ts:
```typescript
export interface SynergyBuff { tileIndex: number; type: string; value: number; }
export function resolveAdjacencySynergies(tiles: TileSlot[]): SynergyBuff[];
```

<!-- From Plan 02 -->
From src/systems/ShopSystem.ts:
```typescript
export class ShopSystem {
  static getShopCards(runState, availableCardIds: string[]): ShopCard[];
  static buyCard(runState, cardId: string, price: number): boolean;
  static removeCard(runState, cardIndex: number): boolean;
  static getRemoveCardCost(deckSize: number): number;
  static startReorderSession(runState): boolean;
  static reorderCard(runState, fromIndex: number, toIndex: number): void;
  static getShopRelics(runState, availableRelicIds: string[]): ShopRelic[];
  static buyRelic(runState, relicId: string, price: number): boolean;
  static sellTile(runState, tileType: string): boolean;
}
```

From src/systems/RestSiteSystem.ts:
```typescript
export type RestChoice = 'rest' | 'train' | 'meditate';
export function applyRestChoice(choice: RestChoice, runState): RestResult;
export function getRestChoices(): Array<{ id: RestChoice; name: string; description: string; }>;
```

From src/systems/EventResolver.ts:
```typescript
export function getRandomEvent(): EventDefinition;
export function isChoiceAvailable(choice: EventChoice, runState): boolean;
export function resolveEventChoice(eventId: string, choiceIndex: number, runState): EventOutcome;
```

From src/systems/TreasureSystem.ts:
```typescript
export function openTreasure(runState, loopCount: number): TreasureResult;
```

From src/systems/BossSystem.ts:
```typescript
export function triggerBossCombat(runState): BossEncounterData;
export function onBossVictory(runState): { metaLootAwarded: number; };
export function getBossExitChoiceData(runState): { safeExitReward: RunEndResult; continueRisk: string; };
```

<!-- UI-SPEC key dimensions -->
Canvas: 800x600
Tile size: 80x80 (world), 40x40 (planning miniature at 50% scale)
HUD: left panel 260x90 at (10,10), right-side info at x=600
Colors: see 03-UI-SPEC.md color table
Font: system stack "Inter, system-ui, Avenir, Helvetica, Arial, sans-serif"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TileVisual + LoopHUD + LoopCelebration UI components + GameScene with LoopRunner integration</name>
  <files>
    src/ui/TileVisual.ts,
    src/ui/LoopHUD.ts,
    src/ui/LoopCelebration.ts,
    src/scenes/GameScene.ts,
    src/main.ts
  </files>
  <read_first>
    src/systems/LoopRunner.ts,
    src/systems/TileRegistry.ts,
    src/systems/SynergyResolver.ts,
    src/systems/DifficultyScaler.ts,
    src/objects/MapManager.ts,
    src/objects/Player.ts,
    src/scenes/Game.ts,
    src/main.ts,
    src/data/tiles.json,
    .planning/phases/03-loop-tile-world/03-UI-SPEC.md
  </read_first>
  <action>
    1. Create src/ui/TileVisual.ts -- reusable Phaser Container for rendering a single tile:
       - Class TileVisual extends Phaser.GameObjects.Container
       - Constructor takes (scene, x, y, tileSlot: TileSlot, scale: number = 1)
       - Contains: Rectangle (80*scale x 80*scale), filled with color from getTileConfig(tileSlot.terrain ?? tileSlot.type).color
       - Basic tiles alternate shade: if index is even use 0x666666, odd use 0x888888 (pass index as optional param)
       - Icon text centered: getTileConfig().icon, Body (16*scale px), white
       - Stroke: 2px default (transparent), white on hover, accent (0xffd700) on selection
       - Synergy indicator: 4px colored strip (0xff00ff) on left/right edge when synergy active (method `setSynergyEdge(side: 'left'|'right'|'none')`)
       - Hover: `setInteractive()`, pointerover sets stroke white, pointerout resets
       - Export class TileVisual

    2. Create src/ui/LoopHUD.ts -- fixed HUD overlay for GameScene:
       - Class LoopHUD extends Phaser.GameObjects.Container
       - Constructor takes (scene: Phaser.Scene)
       - setScrollFactor(0) and setDepth(100) so it stays fixed on screen
       - Left panel: Rectangle 260x90 at (10,10), fill 0x000000 alpha 0.5
         - Row 1 (y=20): Diamond glyph (accent 0xffd700, 20px) + gold amount text (Body 16px, accent)
         - Row 2 (y=42): "Loop {N}" text (Body 16px, white) + "x{mult}" difficulty text (Label 14px, 0xaaaaaa, right-aligned within panel)
         - Row 3 (y=62): HP bar (Rectangle 160x12, semantic color based on HP%) + HP text (Label 14px, white)
       - Right-side (x=600):
         - Tile points: "TP" icon + "{N} TP" text (Body 16px, 0x00e5ff)
         - Meta-loot: star icon + "{N}" text (Label 14px, 0xe040fb)
       - Method `update(runState)`: refresh all text values and HP bar width/color from runState
       - HP bar colors: >50% = 0x00ff00, 25-50% = 0xffaa00, <25% = 0xff0000
       - Export class LoopHUD

    3. Create src/ui/LoopCelebration.ts -- in-world overlay for loop completion:
       - Class LoopCelebration
       - Method `play(scene: Phaser.Scene, loopNumber: number, tilePointsEarned: number)`
       - "LOOP {N} COMPLETE" text at Display (32px bold), accent (0xffd700), center screen (400, 280)
       - Animation: scale 0 to 1.2 over 300ms (Back.easeOut tween), hold 800ms, fade alpha to 0 over 400ms
       - "+{N} Tile Points" at Heading (24px bold), 0x00e5ff, at (400, 320)
       - Fade in 200ms after loop text, float up 20px and fade over 600ms
       - Total duration ~1.5s. After completion, call a callback (passed as param) to transition to planning
       - Export class LoopCelebration

    4. Create src/scenes/GameScene.ts -- thin wrapper over LoopRunner:
       - Register as 'GameScene' in Phaser
       - In `create()`:
         - Get RunState via `getRun()` (Phase 1 pattern: centralized state accessor)
         - Create LoopRunner instance with emit callback that routes to EventBus
         - Call loopRunner.startRun(runState)
         - Create hero sprite: simple colored rectangle 32x48 at screen x=100, y=410 (above tile ground level y=450). Use 0x00aaff fill. Can reuse Player sprite pattern from existing Player.ts if available.
         - Set up camera follow: `this.cameras.main.startFollow(heroSprite, true, 0.1, 0.1)` with deadzone
         - Create LoopHUD instance, add to scene
         - Create tile rendering pool: maintain ~30 TileVisual objects (visible + 10 buffer each direction)
         - Listen for EventBus events:
           - 'combat-start': pause this scene, launch 'CombatScene' (Phase 2)
           - 'open-scene': pause this scene, launch event.scene (ShopScene, RestSiteScene, EventScene, TreasureScene)
           - 'loop-completed': play LoopCelebration, then launch PlanningOverlay
           - 'boss-defeated': pause this scene, launch 'BossExitScene'
           - 'planning-phase-started': handled by loop-completed celebration -> PlanningOverlay
       - In `update(time, delta)`:
         - Call loopRunner.tick(delta)
         - Update hero world position: heroSprite.x = worldOffset + runState.loop.positionInLoop (worldOffset is cumulative to prevent visual jumps on loop wrap -- hero moves continuously forward, tiles cycle via modulo)
         - Update tile visuals: for each visible tile index, position TileVisual at (tileIndex * 80 + worldOffset, 450). Use `tileIndex % runState.loop.tiles.length` to cycle tile data. Generate ahead, clean up behind (existing MapManager pattern).
         - Update LoopHUD with current runState
       - On resume from overlay scenes: call loopRunner.resumeTraversal()
       - Background: fill with 0x1a1a2e

    5. Update src/main.ts:
       - Read existing main.ts to understand current scene registration
       - Add imports for: GameScene, PlanningOverlay, ShopScene, RestSiteScene, EventScene, TreasureScene, BossExitScene
       - Register all 7 new scenes in Phaser config scene array
       - Keep existing scenes that are still needed (CombatScene from Phase 2, etc.)
       - Canvas size: 800x600

    Implementation notes for seamless loop wrap (from RESEARCH pitfall #1):
    - Hero NEVER teleports backward. The hero's world X position increases continuously.
    - Tiles repeat because tileData is indexed by `globalTileIndex % loopLength`.
    - When positionInLoop resets to 0 in LoopRunner, the hero's visual world position does NOT reset.
    - Maintain a `worldOffset` that accumulates: on each loop wrap, worldOffset += loopLength * TILE_SIZE.
    - Tile rendering: for globalIndex near hero, calculate tileDataIndex = globalIndex % loopLength, render tile color/icon from runState.loop.tiles[tileDataIndex].

    Implementation notes for tile pool (from RESEARCH pitfall #6):
    - Only ~30 TileVisual GameObjects exist at any time
    - As hero moves right, create new tiles ahead (heroGlobalIndex + 15) and destroy tiles behind (heroGlobalIndex - 15)
    - Use a Map<number, TileVisual> keyed by globalIndex for easy lookup and cleanup
  </action>
  <verify>
    <automated>cd "C:/Users/nicolas.pereira/OneDrive - Way2 Technology/Área de Trabalho/autoscroller" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/TileVisual.ts contains `export class TileVisual extends Phaser.GameObjects.Container`
    - src/ui/TileVisual.ts contains `setSynergyEdge`
    - src/ui/LoopHUD.ts contains `export class LoopHUD` with `update(` method
    - src/ui/LoopHUD.ts contains `setScrollFactor(0)` and `setDepth(100)`
    - src/ui/LoopCelebration.ts contains `export class LoopCelebration` with `play(` method
    - src/scenes/GameScene.ts contains `import.*LoopRunner` and `loopRunner.tick(` in update method
    - src/scenes/GameScene.ts contains `cameras.main.startFollow`
    - src/scenes/GameScene.ts contains `getRun()` call in create
    - src/scenes/GameScene.ts does NOT contain combat logic or deck management logic (thin wrapper only)
    - src/main.ts registers GameScene, PlanningOverlay, ShopScene, RestSiteScene, EventScene, TreasureScene, BossExitScene
    - `npx tsc --noEmit` produces 0 errors (or only pre-existing errors unrelated to new files)
  </acceptance_criteria>
  <done>GameScene renders hero autoscrolling through tiles with LoopRunner driving movement. HUD displays all run stats. Loop completion celebration plays. Tile pool efficiently manages visible tile objects. All scenes registered in main.ts.</done>
</task>

<task type="auto">
  <name>Task 2: PlanningOverlay + all 5 special tile overlay scenes + BossExitScene</name>
  <files>
    src/scenes/PlanningOverlay.ts,
    src/scenes/ShopScene.ts,
    src/scenes/RestSiteScene.ts,
    src/scenes/EventScene.ts,
    src/scenes/TreasureScene.ts,
    src/scenes/BossExitScene.ts
  </files>
  <read_first>
    src/scenes/GameScene.ts,
    src/systems/LoopRunner.ts,
    src/systems/ShopSystem.ts,
    src/systems/RestSiteSystem.ts,
    src/systems/EventResolver.ts,
    src/systems/TreasureSystem.ts,
    src/systems/BossSystem.ts,
    src/systems/SynergyResolver.ts,
    src/ui/TileVisual.ts,
    src/data/tiles.json,
    src/data/events.json,
    src/data/rest-config.json,
    .planning/phases/03-loop-tile-world/03-UI-SPEC.md
  </read_first>
  <action>
    All overlay scenes follow the same pattern from RESEARCH Pattern 2:
    - Launched on top of paused GameScene: `scene.pause('GameScene'); scene.launch('OverlayName');`
    - Read RunState via `getRun()` in `create()`
    - Mutate RunState directly through system calls
    - Close: `this.scene.stop(); this.scene.resume('GameScene');`
    - Only one overlay open at a time

    1. Create src/scenes/PlanningOverlay.ts:
       - Semi-transparent overlay: fullscreen Rectangle 800x600, 0x000000 alpha 0.7
       - Title: "Planning Phase" at Heading (24px bold), white, centered at y=30
       - Instruction: "Place tiles on empty slots to shape your loop." Body (16px), 0xaaaaaa, centered at y=58
       - Loop layout strip at y=160:
         - Horizontal row of TileVisual at 50% scale (40x40) with 4px gap
         - For 15 tiles: total width = 15*40 + 14*4 = 656px, centered at x=(800-656)/2 = 72
         - For 30+ tiles: implement horizontal drag scroll -- track pointer drag delta, clamp scroll offset
         - Empty/basic slots: render with dashed 2px border in 0xaaaaaa. Make interactive (clickable).
         - Occupied tiles: filled with tile color from TileRegistry, non-interactive
         - When a basic slot is clicked AND a tile type is selected from inventory: call loopRunner.placeTile(slotIndex, selectedTileKey). If success, refresh the slot visual. If fail (slot occupied), show "This slot already has a tile." toast.
         - After each placement, recalculate synergies via resolveAdjacencySynergies() and update synergy edge indicators on all tiles
       - Tile inventory panel at y=300:
         - Rectangle 700x200 at 0x222222 alpha 0.85, centered horizontally (x=50)
         - "Tile Inventory" text at Heading (24px bold), top-left with 16px padding
         - Tile point balance: "{N} TP" Heading (24px), 0x00e5ff, top-right
         - Tile cards: horizontal row for each placeable type from getAllPlaceableTiles()
           - Each card: Rectangle 80x120, 0x333333
           - Top: 40x40 colored square (tile color)
           - Name: Label 14px white
           - Cost: Label 14px, 0x00e5ff, "{cost} TP"
           - If tile exists in tileInventory (free drops): show "x{count}" Label 14px 0xaaaaaa
           - On click: select this tile type (accent stroke 0xffd700, scale 1.05). Deselect previous.
           - If insufficient tile points AND no free copies: dim card to alpha 0.4, cost text in 0xff0000
       - "Start Loop" button at y=540: Heading 24px bold, accent color, centered
         - On click: call loopRunner.confirmPlanning(). Fade overlay out (300ms tween alpha 0->invisible). Stop scene, resume GameScene.

    2. Create src/scenes/ShopScene.ts (extends Phase 2 shop with tile selling + relic buying):
       - Overlay panel: centered Rectangle 650x500 at 0x222222 alpha 0.9
       - Title: "Shop" at Heading (24px bold), 0xffd700, centered top
       - Tab system or sections (vertical scroll):
         - **Buy Cards** section: show 3 cards from ShopSystem.getShopCards(). Each card: Rectangle 120x60, card name + price "60 Gold". Click to buy (calls ShopSystem.buyCard()). Gray out if insufficient gold.
         - **Remove Cards** section: show deck cards list. Each card shows removal cost from ShopSystem.getRemoveCardCost(). Click to remove (calls ShopSystem.removeCard()). Gray out if insufficient gold or deck <= 3.
         - **Reorder Deck** section: "Pay 30 Gold to reorder" button. On pay, show deck as draggable list. Drag-and-drop calls ShopSystem.reorderCard(). Use Phaser drag events (setInteractive + drag listeners).
         - **Buy Relics** section: show 1-2 relics from ShopSystem.getShopRelics(). Each: name + price "150 Gold". Click to buy. Gray out if insufficient gold.
         - **Sell Tiles** section: "Sell Tiles" heading. Show tiles from tileInventory. Each: tile name + "Sell ({points} TP)" button. On click: calls ShopSystem.sellTile(). Refresh inventory display.
       - Gold balance shown at top-right: diamond icon + gold amount (accent)
       - Tile point balance shown below gold: TP icon + amount (0x00e5ff)
       - "Leave Shop" button at bottom: stop scene, resume GameScene, call loopRunner.resumeTraversal()

    3. Create src/scenes/RestSiteScene.ts:
       - Overlay panel: centered Rectangle 500x350 at 0x222222 alpha 0.9
       - Title: "Rest Site" at Heading (24px bold), 0x4169e1, centered top with 24px padding
       - Instruction: "Choose one action." Body (16px), 0xaaaaaa, below title
       - Three option cards, horizontal layout (from getRestChoices()):
         - Each card: Rectangle 140x180 at 0x333333
         - Option name: Heading (24px bold), white, top with 16px padding
         - Description: Body (16px), 0xaaaaaa, center, word-wrap at 120px
         - 24px gap between cards. Entrance: 300ms staggered (add 100ms delay per card tween from alpha 0, x offset -20)
         - Hover: stroke white, scale 1.03 tween (100ms)
         - Click to select: accent stroke 3px, unselected dim to alpha 0.5
       - "Choose" button: Heading (24px bold), accent, below cards with 24px gap. Only visible when an option is selected.
       - On confirm: call applyRestChoice(selectedChoice, runState). Fade cards out (300ms). Show result text at center: Body (16px), 0x00ff00 for heal, white for others. Auto-close after 2s or on click: stop scene, resume GameScene, call loopRunner.resumeTraversal().

    4. Create src/scenes/EventScene.ts:
       - Overlay panel: centered Rectangle 550x400 at 0x222222 alpha 0.9
       - Title: "Event" at Heading (24px bold), 0x9370db, centered top
       - Get random event: call getRandomEvent(). Store eventId.
       - Narrative text: Body (16px), white, left-aligned with 24px padding, word-wrap at 500px
       - Choice buttons (vertical stack):
         - Each choice: Rectangle 480x44 at 0x333333, text Body (16px) left-aligned with 16px padding
         - 8px gap between choices. Entrance: 200ms staggered slide-in from right.
         - Check isChoiceAvailable() for each: if unavailable, dim to alpha 0.4, append "(Requires: {requirement})" in 0xff0000
         - Hover (if available): fill lightens to 0x444444
         - Click: accent stroke, other choices fade out (300ms)
       - On choice click: call resolveEventChoice(eventId, choiceIndex, runState)
         - Replace choices with outcome panel: Body (16px), white text. Positive effects prefix "+" in 0x00ff00. Negative effects prefix "-" in 0xff0000.
         - "Continue" button: Heading (24px bold), accent, bottom center
       - On continue: stop scene, resume GameScene, call loopRunner.resumeTraversal()

    5. Create src/scenes/TreasureScene.ts:
       - Overlay panel: centered Rectangle 500x350 at 0x222222 alpha 0.9
       - Title: "Treasure!" at Heading (24px bold), 0xff8c00, centered top
       - "You found:" Body (16px), 0xaaaaaa, below title
       - Call openTreasure(runState, runState.loop.count) to get TreasureResult
       - Loot items (vertical list):
         - Each item: text row with 24px circle icon (color by type: gold=0xffd700, card=white, relic=0x00ccff, tile=tile color) + item name Body (16px) white + value Label (14px) 0xaaaaaa right-aligned
         - 8px gap between items
         - Staggered entrance: 200ms per item, slide right 20px + fade in
       - "Take All" button: Heading (24px bold), accent, bottom center. Appears after last item animates in.
       - On Take All click: items slide left + fade out (150ms staggered). Then stop scene, resume GameScene, call loopRunner.resumeTraversal().
       - Empty state (0 items): "Empty Chest" Heading (24px) 0xaaaaaa + "The chest was already looted." Body (16px) 0xaaaaaa. "Continue" button instead of "Take All".

    6. Create src/scenes/BossExitScene.ts:
       - Overlay panel: centered Rectangle 600x400 at 0x222222 alpha 0.9
       - Title: "Boss Defeated!" at Display (32px bold), 0xffd700, centered at top with 24px padding
       - Call getBossExitChoiceData(runState) to get reward preview data
       - Two option panels side by side, centered with 32px gap:
         - Left ("Exit Run"): Rectangle 240x220 at 0x1a3a1a (dark green). Label: Heading (24px bold), 0x00ff00. Description: Body (16px) white "Bank all meta-loot and XP safely." Reward preview: Label (14px), 0x00ff00, "+{metaLoot} Meta-Loot, +{xp} XP".
         - Right ("Continue"): Rectangle 240x220 at 0x3a1a1a (dark red). Label: Heading (24px bold), 0xff6600. Description: Body (16px) white "The loop grows by 3 tiles. Risk everything." Warning: Label (14px), 0xff0000, "Death means 25% meta-loot, zero XP."
         - Hover: stroke brightens to respective color (0x00ff00 or 0xff6600), scale 1.03 tween
         - Click to select: 3px accent stroke, unselected dims to alpha 0.6
       - Confirm button: appears below selected panel. Heading (24px bold), color matches selection.
       - On confirm "Exit Run": call loopRunner.onBossChoice('exit'). Stop this scene. Transition to run summary/death screen (Phase 2's GameOverScene or equivalent) with safe exit data.
       - On confirm "Continue": call loopRunner.onBossChoice('continue'). Stop this scene. Resume GameScene. (LoopRunner will be in 'planning' state, so GameScene detects this and launches PlanningOverlay.)

    Scene Z-ordering and pause pattern (from RESEARCH pitfall #5):
    - All overlays: `this.scene.pause('GameScene')` on launch
    - On close: `this.scene.stop()` self, then `this.scene.resume('GameScene')`
    - GameScene in update(): check if LoopRunner state is 'planning' and no overlay active -> launch PlanningOverlay
  </action>
  <verify>
    <automated>cd "C:/Users/nicolas.pereira/OneDrive - Way2 Technology/Área de Trabalho/autoscroller" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/scenes/PlanningOverlay.ts contains `loopRunner.placeTile(` and `loopRunner.confirmPlanning(` and `resolveAdjacencySynergies(`
    - src/scenes/PlanningOverlay.ts contains `getAllPlaceableTiles()` call for inventory display
    - src/scenes/ShopScene.ts contains `ShopSystem.buyCard` and `ShopSystem.removeCard` and `ShopSystem.sellTile` and `ShopSystem.buyRelic`
    - src/scenes/RestSiteScene.ts contains `applyRestChoice(` and `getRestChoices(`
    - src/scenes/EventScene.ts contains `getRandomEvent()` and `resolveEventChoice(` and `isChoiceAvailable(`
    - src/scenes/TreasureScene.ts contains `openTreasure(`
    - src/scenes/BossExitScene.ts contains `getBossExitChoiceData(` and `loopRunner.onBossChoice(`
    - All 6 scene files contain `this.scene.stop()` and `this.scene.resume('GameScene')` (overlay close pattern)
    - `npx tsc --noEmit` produces 0 errors (or only pre-existing errors unrelated to new files)
  </acceptance_criteria>
  <done>All 6 overlay scenes implemented as thin Phaser wrappers. PlanningOverlay enables tile placement with synergy indicators. ShopScene provides full deck/relic/tile management. RestSiteScene, EventScene, TreasureScene, and BossExitScene all follow the overlay pattern and delegate logic to pure TS systems.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete loop gameplay</name>
  <files>n/a</files>
  <action>
    Human verifies the full loop + tile world gameplay works end-to-end in the browser.
    Steps: (1) npm run dev, (2) hero autoscrolls through tiles, (3) HUD shows loop/gold/TP/meta-loot/HP/difficulty,
    (4) loop completion celebration plays, (5) PlanningOverlay with tile grid and inventory,
    (6) tile placement on empty slots, (7) synergy indicators on adjacent tiles,
    (8) Start Loop resumes autoscroll, (9) terrain tiles trigger combat,
    (10) shop overlay with deck mgmt, (11) rest site with 3 choices,
    (12) event with narrative and choices, (13) treasure with loot and Take All,
    (14) boss tile at loop 5, (15) boss exit choice green vs red panels,
    (16) all overlays pause/resume GameScene correctly.
  </action>
  <verify>Human approval after testing all 16 verification steps</verify>
  <done>Human verified: loop gameplay works end-to-end with autoscroll, HUD, planning, all overlays, boss exit, seamless loop wrap.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `npx tsc --noEmit` -- 0 new errors
2. Full test suite: `npx vitest run --reporter=verbose` -- all tests pass
3. Dev server: `npm run dev` -- game loads and runs without console errors
4. Visual verification: hero autoscrolls, HUD updates, tiles render, overlays open/close, loop wraps seamlessly
</verification>

<success_criteria>
- GameScene renders hero autoscrolling through tiles with LoopRunner driving all logic
- LoopHUD displays loop counter, gold, tile points, meta-loot, HP, difficulty
- Loop completion celebration plays between loops
- PlanningOverlay enables strategic tile placement with synergy feedback
- All 5 special tile overlays (Shop, Rest, Event, Treasure, Boss Exit) function correctly
- Boss exit choice presents clear safe (green) vs risk (red) decision
- All overlays use pause/resume pattern -- no input bleed or stacking
- Tile pool efficiently manages ~30 visible tiles with generate-ahead/cleanup-behind
- Seamless loop wrap -- no visual jump when loop cycles
- Human verification confirms playable loop gameplay
</success_criteria>

<output>
After completion, create `.planning/phases/03-loop-tile-world/03-03-SUMMARY.md`
</output>
