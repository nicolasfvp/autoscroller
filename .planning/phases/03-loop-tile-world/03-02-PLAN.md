---
phase: 03-loop-tile-world
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/systems/ShopSystem.ts
  - src/systems/RestSiteSystem.ts
  - src/systems/EventResolver.ts
  - src/systems/TreasureSystem.ts
  - src/systems/BossSystem.ts
  - src/data/events.json
  - src/data/treasure-tables.json
  - src/data/rest-config.json
  - tests/systems/ShopSystem.test.ts
  - tests/systems/RestSiteSystem.test.ts
  - tests/systems/EventResolver.test.ts
  - tests/systems/TreasureSystem.test.ts
  - tests/systems/BossSystem.test.ts
autonomous: true
requirements:
  - LOOP-02
  - TILE-03
  - TILE-05
  - SPEC-01
  - SPEC-02
  - SPEC-03
  - SPEC-04
  - SPEC-05

must_haves:
  truths:
    - "Shop system supports buying cards, removing cards, reordering deck, buying relics, and selling tile drops for 50% tile points"
    - "Rest site offers 3 choices: recover 30% HP, train (+2 damage to random card), meditate (+5 max stamina or mana)"
    - "Event resolver loads events from JSON, validates choice requirements, and applies effects to RunState"
    - "Treasure system rolls 1-3 loot items and adds them all to RunState"
    - "Boss system manages boss combat trigger, exit choice flow, and loop growth on continue"
    - "Tile placement works only during planning phase on empty (basic) slots"
  artifacts:
    - path: "src/systems/ShopSystem.ts"
      provides: "Shop operations: buy card, remove card, reorder deck, buy relic, sell tile"
      exports: ["ShopSystem"]
    - path: "src/systems/RestSiteSystem.ts"
      provides: "Rest site 3-choice system"
      exports: ["applyRestChoice", "RestChoice"]
    - path: "src/systems/EventResolver.ts"
      provides: "Event choice resolution from JSON"
      exports: ["EventResolver", "resolveEventChoice"]
    - path: "src/systems/TreasureSystem.ts"
      provides: "Treasure loot roll and application"
      exports: ["openTreasure", "TreasureResult"]
    - path: "src/systems/BossSystem.ts"
      provides: "Boss encounter trigger and exit choice flow"
      exports: ["BossSystem"]
    - path: "src/data/events.json"
      provides: "5 event definitions with choices and effects"
      contains: "mysterious_merchant"
    - path: "src/data/treasure-tables.json"
      provides: "Treasure loot table weights and ranges"
      contains: "goldWeight"
    - path: "src/data/rest-config.json"
      provides: "Rest site config values"
      contains: "hpRecoveryPercent"
  key_links:
    - from: "src/systems/ShopSystem.ts"
      to: "RunState"
      via: "Mutates economy.gold, economy.tilePoints, deck, tileInventory"
      pattern: "run\\.economy"
    - from: "src/systems/EventResolver.ts"
      to: "src/data/events.json"
      via: "Loads event definitions, applies choice effects"
      pattern: "events\\.json"
    - from: "src/systems/RestSiteSystem.ts"
      to: "RunState"
      via: "Mutates hero.hp, hero.maxStamina, hero.maxMana, deck.cards"
      pattern: "run\\.hero"
    - from: "src/systems/BossSystem.ts"
      to: "src/systems/LoopRunner.ts"
      via: "Calls onBossDefeated() and onBossChoice()"
      pattern: "loopRunner\\.onBossChoice"
---

<objective>
Build all special tile pure TypeScript systems (Shop, Rest, Event, Treasure, Boss) and their JSON data configs. These systems read/mutate RunState and are called by overlay scenes in Plan 03.

Purpose: Special tiles are the strategic decision points in the loop. Shop handles deck management + tile economy, rest offers HP/train/meditate, events provide narrative risk/reward, treasure gives guaranteed loot, and boss manages the highest-stakes exit decision.

Output: 5 pure TS system files, 3 JSON data files, 5 test files. All tests pass.
</objective>

<execution_context>
@C:/Users/nicolas.pereira/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/nicolas.pereira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-loop-tile-world/03-CONTEXT.md
@.planning/phases/03-loop-tile-world/03-RESEARCH.md
@.planning/phases/03-loop-tile-world/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/systems/TileRegistry.ts:
```typescript
export type TileSlotType = 'basic' | 'terrain' | 'shop' | 'rest' | 'event' | 'treasure' | 'boss';
export type TerrainType = 'forest' | 'graveyard' | 'swamp';
export interface TileConfig { type: TileSlotType; terrain?: TerrainType; name: string; color: number; canPlaceManually: boolean; tilePointCost: number; icon: string; combatChance?: number; }
export interface TileSlot { type: TileSlotType; terrain?: TerrainType; defeatedThisLoop: boolean; }
export interface TileInventoryEntry { tileType: string; count: number; }
export function getTileConfig(key: string): TileConfig;
export function getAllPlaceableTiles(): TileConfig[];
```

From src/systems/LoopRunner.ts:
```typescript
export class LoopRunner {
  getState(): LoopState;
  startRun(runState): void;
  tick(delta: number): void;
  resumeTraversal(): void;
  confirmPlanning(): void;
  onBossDefeated(): void;
  onBossChoice(choice: 'exit' | 'continue'): void;
  placeTile(slotIndex: number, tileKey: string): boolean;
  getActiveBuffs(): SynergyBuff[];
}
```

From src/systems/LootGenerator.ts:
```typescript
export function rollTreasureLoot(loopCount: number): LootResult;
export function rollMetaLoot(source: 'combat' | 'loop' | 'boss', loopCount: number): number;
```

From src/systems/RunEndResolver.ts:
```typescript
export function resolveRunEnd(exitType: 'safe' | 'death', currentMetaLoot: number, currentXp: number): RunEndResult;
```

<!-- RunState shape (same as Plan 01) -->
RunState:
```typescript
interface RunState {
  hero: { hp: number; maxHp: number; stamina: number; maxStamina: number; mana: number; maxMana: number; xp: number; };
  deck: { cards: CardInstance[]; order: string[]; };
  loop: { count: number; length: number; tiles: TileSlot[]; positionInLoop: number; difficultyMultiplier: number; };
  economy: { gold: number; tilePoints: number; metaLoot: number; };
  tileInventory: TileInventoryEntry[];
  relics: string[];
}
```

From src/data/EventDefinitions.ts (existing, to be migrated to events.json):
```typescript
export type EventChoiceEffect = 'gain_hp' | 'lose_hp' | 'gain_gold' | 'lose_gold' | 'add_card' | 'remove_card' | 'gain_relic' | 'add_curse';
export interface EventChoice {
    text: string;
    effects: Array<{ type: EventChoiceEffect; value?: number | string; }>;
    requirement?: { minGold?: number; minHP?: number; };
}
export interface EventDefinition {
    id: string; title: string; description: string; choices: EventChoice[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ShopSystem + RestSiteSystem + JSON configs + tests</name>
  <files>
    src/systems/ShopSystem.ts,
    src/systems/RestSiteSystem.ts,
    src/data/rest-config.json,
    tests/systems/ShopSystem.test.ts,
    tests/systems/RestSiteSystem.test.ts
  </files>
  <read_first>
    src/systems/TileRegistry.ts,
    src/systems/LoopRunner.ts,
    src/data/tiles.json,
    src/data/DifficultyConfig.ts,
    src/data/EnemyDrops.ts,
    src/data/CardDefinitions.ts
  </read_first>
  <behavior>
    ShopSystem:
    - getShopCards(runState) returns 3 random cards with gold prices (base 60 gold per card)
    - buyCard(runState, cardId) deducts gold, adds card to deck. Returns false if insufficient gold
    - removeCard(runState, cardIndex) deducts gold (cost = 75 / deckSize, rounded up), removes card. Returns false if insufficient gold or deck size <= 3
    - startReorderSession(runState) deducts flat 30 gold, returns true. Returns false if insufficient gold
    - reorderCard(runState, fromIndex, toIndex) moves card in deck order array (only valid after startReorderSession)
    - getShopRelics(runState) returns 1-2 random relics not already owned, each costing 150 gold
    - buyRelic(runState, relicId) deducts 150 gold, adds relic to relics array
    - sellTile(runState, tileType) removes 1 tile from tileInventory, adds 50% of tile's tilePointCost to economy.tilePoints. Returns false if tile not in inventory
    - Selling a forest tile (cost 3 TP) gives floor(3*0.5) = 1 TP
    - Selling a treasure tile (cost 6 TP) gives floor(6*0.5) = 3 TP

    RestSiteSystem:
    - applyRestChoice('rest', runState) heals hero.hp by 30% of maxHp (rounded down), capped at maxHp
    - applyRestChoice('train', runState) picks a random card from deck.cards and increases its damage by +2 (adds a 'trainBonus' property or modifies card stats)
    - applyRestChoice('meditate', runState) increases hero.maxStamina by +5 OR hero.maxMana by +5 (50/50 random)
    - Each choice returns a description string of what happened
  </behavior>
  <action>
    1. Create src/data/rest-config.json:
       ```json
       {
         "hpRecoveryPercent": 0.30,
         "trainDamageBonus": 2,
         "meditateBonusAmount": 5,
         "choices": [
           { "id": "rest", "name": "Rest", "description": "Recover 30% of max HP." },
           { "id": "train", "name": "Train", "description": "Boost a random card's damage by +2." },
           { "id": "meditate", "name": "Meditate", "description": "Increase max stamina or mana by +5." }
         ]
       }
       ```

    2. Create src/systems/ShopSystem.ts:
       - Import getTileConfig from TileRegistry
       - Export type `ShopCard = { cardId: string; name: string; price: number; }`
       - Export type `ShopRelic = { relicId: string; name: string; price: number; }`
       - Export class ShopSystem with static methods (pure functions operating on RunState):
         - `static getShopCards(runState, availableCardIds: string[]): ShopCard[]`
           - Pick 3 random from availableCardIds, price = 60 gold each
         - `static buyCard(runState, cardId: string, price: number): boolean`
           - If economy.gold < price return false. Deduct gold. Add cardId to deck.order. Return true.
         - `static getRemoveCardCost(deckSize: number): number`
           - Returns Math.ceil(75 / deckSize). Minimum deck size to remove is 4 (can't go below 3).
         - `static removeCard(runState, cardIndex: number): boolean`
           - If deck.order.length <= 3 return false. Cost = getRemoveCardCost(deck.order.length). If economy.gold < cost return false. Deduct gold. Remove card at cardIndex from deck.order. Return true.
         - `static startReorderSession(runState): boolean`
           - If economy.gold < 30 return false. Deduct 30 gold. Return true.
         - `static reorderCard(runState, fromIndex: number, toIndex: number): void`
           - Splice card from fromIndex, insert at toIndex in deck.order array.
         - `static getShopRelics(runState, availableRelicIds: string[]): ShopRelic[]`
           - Filter out relics already in runState.relics. Pick 1-2 random. Price = 150 gold each.
         - `static buyRelic(runState, relicId: string, price: number): boolean`
           - If economy.gold < price return false. Deduct gold. Push relicId to relics. Return true.
         - `static sellTile(runState, tileType: string): boolean`
           - Find tileType in tileInventory. If not found or count<=0 return false. Decrement count (remove entry if 0). Add floor(getTileConfig(tileType).tilePointCost * 0.5) to economy.tilePoints. Return true.

    3. Create src/systems/RestSiteSystem.ts:
       - Import rest-config.json
       - Export type `RestChoice = 'rest' | 'train' | 'meditate'`
       - Export interface `RestResult { choice: RestChoice; description: string; }`
       - Export function `applyRestChoice(choice: RestChoice, runState): RestResult`
         - 'rest': heal = floor(runState.hero.maxHp * 0.30). runState.hero.hp = min(runState.hero.hp + heal, runState.hero.maxHp). Return description "Recovered {heal} HP."
         - 'train': pick random index from deck.cards. If the card has a `bonusDamage` field, increment it by 2; otherwise set it to 2. Return description "Boosted {cardName} damage by +2."
         - 'meditate': 50/50 random. Either runState.hero.maxStamina += 5 or runState.hero.maxMana += 5. Return "Increased max stamina by +5." or "Increased max mana by +5."
       - Export function `getRestChoices(): Array<{ id: RestChoice; name: string; description: string; }>` -- returns choices from rest-config.json

    4. Write tests/systems/ShopSystem.test.ts:
       - 'buyCard deducts gold and adds card to deck'
       - 'buyCard returns false when insufficient gold'
       - 'removeCard deducts scaled gold and removes card'
       - 'removeCard returns false when deck size <= 3'
       - 'getRemoveCardCost returns ceil(75/deckSize) -- deck of 10 = 8 gold, deck of 5 = 15 gold'
       - 'startReorderSession deducts 30 gold'
       - 'reorderCard moves card from index to index'
       - 'sellTile gives 50% tile points -- forest (3 TP cost) gives 1 TP'
       - 'sellTile gives 50% tile points -- treasure (6 TP cost) gives 3 TP'
       - 'sellTile returns false when tile not in inventory'
       - 'buyRelic deducts gold and adds relic'

    5. Write tests/systems/RestSiteSystem.test.ts:
       - 'rest choice heals 30% of maxHp'
       - 'rest choice does not exceed maxHp'
       - 'train choice adds +2 bonusDamage to a card'
       - 'meditate choice increases maxStamina or maxMana by 5'
       - 'getRestChoices returns 3 choices'
  </action>
  <verify>
    <automated>cd "C:/Users/nicolas.pereira/OneDrive - Way2 Technology/Área de Trabalho/autoscroller" && npx vitest run tests/systems/ShopSystem.test.ts tests/systems/RestSiteSystem.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/systems/ShopSystem.ts contains `export class ShopSystem` with methods `buyCard`, `removeCard`, `startReorderSession`, `reorderCard`, `sellTile`, `buyRelic`
    - src/systems/ShopSystem.ts contains `getRemoveCardCost` with formula `Math.ceil(75 / deckSize)`
    - src/systems/RestSiteSystem.ts contains `export function applyRestChoice` and `export function getRestChoices`
    - src/data/rest-config.json contains "hpRecoveryPercent" and "trainDamageBonus" and "meditateBonusAmount"
    - tests/systems/ShopSystem.test.ts contains at least 10 test cases
    - tests/systems/RestSiteSystem.test.ts contains at least 4 test cases
    - `npx vitest run tests/systems/ShopSystem.test.ts tests/systems/RestSiteSystem.test.ts` exits with code 0
  </acceptance_criteria>
  <done>ShopSystem handles all deck management + tile economy operations. RestSiteSystem provides 3-choice rest site logic. Both mutate RunState directly and have full test coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EventResolver + TreasureSystem + BossSystem + JSON data + tests</name>
  <files>
    src/systems/EventResolver.ts,
    src/systems/TreasureSystem.ts,
    src/systems/BossSystem.ts,
    src/data/events.json,
    src/data/treasure-tables.json,
    tests/systems/EventResolver.test.ts,
    tests/systems/TreasureSystem.test.ts,
    tests/systems/BossSystem.test.ts
  </files>
  <read_first>
    src/systems/LoopRunner.ts,
    src/systems/LootGenerator.ts,
    src/systems/RunEndResolver.ts,
    src/data/EventDefinitions.ts,
    src/data/EnemyDrops.ts,
    src/data/difficulty.json
  </read_first>
  <behavior>
    EventResolver:
    - getRandomEvent() returns a random EventDefinition from events.json
    - getEvent(id) returns a specific event by ID
    - isChoiceAvailable(choice, runState) returns true if choice requirements are met (minGold, minHP)
    - resolveEventChoice(eventId, choiceIndex, runState) applies all effects of the chosen option to RunState and returns an outcome description string
    - Effects: gain_hp adds to hero.hp (capped at maxHp), lose_hp subtracts from hero.hp (min 1), gain_gold adds to economy.gold, lose_gold subtracts from economy.gold, add_card adds to deck.order, remove_card removes random card, gain_relic adds to relics, add_curse (placeholder - no-op for now)

    TreasureSystem:
    - openTreasure(runState, loopCount) rolls LootGenerator.rollTreasureLoot(), applies all items to RunState, returns TreasureResult with list of items gained
    - Gold items add to economy.gold
    - Card items add cardId to deck.order
    - Relic items add relicId to relics
    - Tile items add to tileInventory

    BossSystem:
    - triggerBossCombat(runState) returns the boss enemy definition scaled for current loop
    - onBossVictory(runState, loopRunner) calls loopRunner.onBossDefeated(), awards meta-loot (10), returns boss defeat data
    - onBossExitChoice(choice, runState, loopRunner) delegates to loopRunner.onBossChoice(choice)
  </behavior>
  <action>
    1. Create src/data/events.json -- migrate existing 5 events from EventDefinitions.ts to JSON format:
       ```json
       [
         {
           "id": "mysterious_merchant",
           "title": "Mysterious Merchant",
           "description": "A hooded figure offers you a strange deal...",
           "choices": [
             {
               "text": "Trade 30 Gold for a random rare card",
               "effects": [
                 { "type": "lose_gold", "value": 30 },
                 { "type": "add_card", "value": "random_rare" }
               ],
               "requirement": { "minGold": 30 }
             },
             {
               "text": "Trade random card for 50 Gold",
               "effects": [
                 { "type": "remove_card", "value": "random" },
                 { "type": "gain_gold", "value": 50 }
               ]
             },
             {
               "text": "Walk away",
               "effects": []
             }
           ]
         },
         {
           "id": "cursed_chest",
           "title": "Cursed Chest",
           "description": "A glowing chest radiates dark energy. Open it?",
           "choices": [
             {
               "text": "Open it (Gain relic + curse)",
               "effects": [
                 { "type": "gain_relic", "value": "random" },
                 { "type": "add_curse", "value": "pain" }
               ]
             },
             {
               "text": "Leave it alone",
               "effects": []
             }
           ]
         },
         {
           "id": "healing_fountain",
           "title": "Healing Fountain",
           "description": "A magical fountain offers healing, but at what cost?",
           "choices": [
             {
               "text": "Drink deeply (Heal 40 HP)",
               "effects": [{ "type": "gain_hp", "value": 40 }]
             },
             {
               "text": "Sip carefully (Heal 20 HP)",
               "effects": [{ "type": "gain_hp", "value": 20 }]
             },
             {
               "text": "Ignore it",
               "effects": []
             }
           ]
         },
         {
           "id": "ancient_shrine",
           "title": "Ancient Shrine",
           "description": "An old shrine glows with power. Make an offering?",
           "choices": [
             {
               "text": "Offer 20 HP for a powerful relic",
               "effects": [
                 { "type": "lose_hp", "value": 20 },
                 { "type": "gain_relic", "value": "rare" }
               ],
               "requirement": { "minHP": 21 }
             },
             {
               "text": "Pray for guidance (+30 Gold)",
               "effects": [{ "type": "gain_gold", "value": 30 }]
             },
             {
               "text": "Leave",
               "effects": []
             }
           ]
         },
         {
           "id": "traveling_salesman",
           "title": "Traveling Salesman",
           "description": "A friendly merchant with questionable goods.",
           "choices": [
             {
               "text": "Buy healing potion (40 Gold, Heal 30 HP)",
               "effects": [
                 { "type": "lose_gold", "value": 40 },
                 { "type": "gain_hp", "value": 30 }
               ],
               "requirement": { "minGold": 40 }
             },
             {
               "text": "Sell a card for 40 Gold",
               "effects": [
                 { "type": "remove_card", "value": "choose" },
                 { "type": "gain_gold", "value": 40 }
               ]
             },
             {
               "text": "Move on",
               "effects": []
             }
           ]
         }
       ]
       ```

    2. Create src/data/treasure-tables.json:
       ```json
       {
         "minItems": 1,
         "maxItems": 3,
         "weights": {
           "gold": 40,
           "card": 30,
           "relic": 10,
           "tile": 20
         },
         "goldRange": { "base": 20, "max": 50, "loopScale": "sqrt" },
         "cardPool": "all_available",
         "relicPool": "all_available",
         "tilePool": "all_placeable"
       }
       ```

    3. Create src/systems/EventResolver.ts:
       - Import events from '../data/events.json' (type EventDefinition[] compatible)
       - Export interface `EventOutcome { description: string; effects: Array<{ type: string; value: number | string; applied: boolean; }>; }`
       - Export function `getRandomEvent(): EventDefinition` -- random from events array
       - Export function `getEvent(id: string): EventDefinition | undefined` -- find by id
       - Export function `getAllEvents(): EventDefinition[]` -- return copy of events array
       - Export function `isChoiceAvailable(choice: EventChoice, runState): boolean`
         - If choice.requirement?.minGold and runState.economy.gold < minGold return false
         - If choice.requirement?.minHP and runState.hero.hp < minHP return false
         - Otherwise return true
       - Export function `resolveEventChoice(eventId: string, choiceIndex: number, runState): EventOutcome`
         - Get event by id. Get choice at choiceIndex.
         - For each effect in choice.effects:
           - 'gain_hp': add value to hero.hp, cap at maxHp
           - 'lose_hp': subtract value from hero.hp, min 1
           - 'gain_gold': add value to economy.gold
           - 'lose_gold': subtract value from economy.gold, min 0
           - 'add_card': add value (cardId string) to deck.order (if 'random_rare', pick random from a pool -- use placeholder 'fury' for now)
           - 'remove_card': if 'random', remove random card from deck.order (only if deck size > 3). If 'choose', mark as pending (return applied:false for this effect).
           - 'gain_relic': add value (relicId) to relics. If 'random' or 'rare', pick placeholder relic id.
           - 'add_curse': no-op placeholder, return applied:false
         - Build and return EventOutcome with description summarizing what happened

    4. Create src/systems/TreasureSystem.ts:
       - Import rollTreasureLoot from LootGenerator, treasure-tables.json for config reference
       - Export interface `TreasureResult { items: Array<{ type: string; name: string; amount?: number; id?: string; }>; }`
       - Export function `openTreasure(runState, loopCount: number): TreasureResult`
         - Call rollTreasureLoot(loopCount) to get LootResult
         - For each item in result.items:
           - type 'gold': add item.amount to economy.gold
           - type 'card': add item.id to deck.order
           - type 'relic': add item.id to relics
           - type 'tile': find or create entry in tileInventory, increment count
         - Return TreasureResult with descriptive item list

    5. Create src/systems/BossSystem.ts:
       - Import scaleEnemyForLoop from DifficultyScaler
       - Import rollMetaLoot from LootGenerator
       - Export interface `BossEncounterData { enemyId: string; scaledStats: ScaledEnemyStats; isBoss: true; }`
       - Export function `triggerBossCombat(runState): BossEncounterData`
         - Use enemy id 'boss_demon' (only boss for now)
         - Scale with scaleEnemyForLoop(bossBaseStats, runState.loop.count, true)
         - Return encounter data
       - Export function `onBossVictory(runState): { metaLootAwarded: number; }`
         - Award meta-loot: metaLoot = rollMetaLoot('boss', runState.loop.count)
         - Add to runState.economy.metaLoot
         - Return { metaLootAwarded: metaLoot }
       - Export function `getBossExitChoiceData(runState): { safeExitReward: RunEndResult; continueRisk: string; }`
         - safeExitReward: resolveRunEnd('safe', runState.economy.metaLoot, runState.hero.xp)
         - continueRisk: "Loop grows by 3 tiles. Death means 25% meta-loot, zero XP."
         - Return both for UI display

    6. Write tests:
       tests/systems/EventResolver.test.ts:
       - 'getRandomEvent returns a valid event with id and choices'
       - 'getEvent returns correct event by id'
       - 'isChoiceAvailable returns false when gold requirement not met'
       - 'isChoiceAvailable returns false when HP requirement not met'
       - 'isChoiceAvailable returns true when no requirements'
       - 'resolveEventChoice gain_hp adds hp capped at maxHp'
       - 'resolveEventChoice lose_gold subtracts gold, min 0'
       - 'resolveEventChoice returns EventOutcome with description'

       tests/systems/TreasureSystem.test.ts:
       - 'openTreasure returns 1-3 items'
       - 'openTreasure gold items add to economy.gold'
       - 'openTreasure tile items add to tileInventory'
       - 'openTreasure returns TreasureResult with item descriptions'

       tests/systems/BossSystem.test.ts:
       - 'triggerBossCombat returns scaled boss stats with isBoss=true'
       - 'onBossVictory awards meta-loot to runState'
       - 'getBossExitChoiceData returns safe exit with 100% meta-loot'
       - 'getBossExitChoiceData returns continue risk warning string'
  </action>
  <verify>
    <automated>cd "C:/Users/nicolas.pereira/OneDrive - Way2 Technology/Área de Trabalho/autoscroller" && npx vitest run tests/systems/EventResolver.test.ts tests/systems/TreasureSystem.test.ts tests/systems/BossSystem.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/data/events.json contains "mysterious_merchant" and "cursed_chest" and "healing_fountain" and "ancient_shrine" and "traveling_salesman" (5 events)
    - src/data/treasure-tables.json contains "goldWeight" or "weights" and "goldRange"
    - src/data/rest-config.json contains "hpRecoveryPercent" with value 0.30
    - src/systems/EventResolver.ts contains `export function resolveEventChoice` and `export function isChoiceAvailable` and `export function getRandomEvent`
    - src/systems/TreasureSystem.ts contains `export function openTreasure`
    - src/systems/BossSystem.ts contains `export function triggerBossCombat` and `export function onBossVictory` and `export function getBossExitChoiceData`
    - `npx vitest run` (full suite) exits with code 0
  </acceptance_criteria>
  <done>All 5 special tile systems implemented as pure TypeScript. Events loaded from JSON with 5 narrative encounters. Treasure rolls loot and applies to RunState. Boss system scales combat, awards meta-loot, and provides exit choice data. Full test coverage.</done>
</task>

</tasks>

<verification>
Run full test suite: `npx vitest run --reporter=verbose`

All test files from Plan 01 + Plan 02 must pass:
- tests/systems/TileRegistry.test.ts
- tests/systems/SynergyResolver.test.ts
- tests/systems/DifficultyScaler.test.ts
- tests/systems/RunEndResolver.test.ts
- tests/systems/LootGenerator.test.ts
- tests/systems/LoopRunner.test.ts
- tests/systems/ShopSystem.test.ts
- tests/systems/RestSiteSystem.test.ts
- tests/systems/EventResolver.test.ts
- tests/systems/TreasureSystem.test.ts
- tests/systems/BossSystem.test.ts

Verify no Phaser imports in any src/systems/ file:
`grep -r "import.*[Pp]haser" src/systems/ | wc -l` should output 0
</verification>

<success_criteria>
- 5 special tile systems created as pure TypeScript with no Phaser dependency
- 3 JSON data files (events, treasure-tables, rest-config)
- 5 test files with 30+ test cases, all passing
- ShopSystem handles all deck management + tile selling operations
- RestSiteSystem provides 3-choice rest logic (rest/train/meditate)
- EventResolver loads 5 events from JSON, validates requirements, applies effects
- TreasureSystem rolls and applies treasure loot to RunState
- BossSystem handles boss combat trigger, meta-loot award, and exit choice data
- Full test suite (Plans 01 + 02) passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-loop-tile-world/03-02-SUMMARY.md`
</output>
